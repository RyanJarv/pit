{
  "name": "pit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Prompt iteration tool for experimenting with project-generating prompts.",
  "bin": {
    "pit": "./src/cli.ts"
  },
  "scripts": {
    "pit": "tsx src/cli.ts",
    "test": "node --import tsx --test tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
